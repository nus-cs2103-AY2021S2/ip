import duke.Apollo;

/**
 * Contains the main method that drives the entire bot.
 */
public class Duke {

    /**
     * Main method that drives the program. Starts up an instance of Apollo.
     * @param args Arguments included when program is invoked.
     */
    public static void main(String[] args) {
        Apollo apollo = new Apollo();
    }
}
