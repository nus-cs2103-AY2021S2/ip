package duke.exception;

/**
 * Enum class which consists of the different types of exception
 */
public enum ExceptionType {
    BLANK_DESCRIPTION, INVALID_INTEGER, INVALID_FORMAT, INVALID_INPUT, INVALID_DATETIME, LOADING_ERROR, SAVING_ERROR,
    INVALID_FILE_CONFIGURATION
}
