package yoda.command;

/**
 * CommandType class to enumerate the types of commands available to handle the user's requests.
 */
public enum CommandType {
    LIST, TODO, DEADLINE, EVENT, DONE, DELETE, FIND, BYE, HELP, ERROR
}
